<tr id="events-info">
   <td>
  <div class="registered-button">
      <form action="/home/{{this._id}}" method="get">
    <button name="register-button" value="Inscrever">Inscrever-se</button>
    </form>
    </div>
  </td>

  <td>{{this.date}}</td>
  <td>{{this.time}}</td>
  <td>
    <a href="/eventPageView/{{this._id}}"> {{this.name}}</a> 
  </td>
  <td>{{this.owner.name}}</td>
</tr>